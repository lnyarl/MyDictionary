# Learnings

## Mobile Navigation
- Created `MobileFooter` component for mobile navigation.
- Used `fixed bottom-0` for positioning.
- Used `backdrop-blur` and `bg-background/95` for modern glassmorphism effect.
- Implemented active state handling with `useLocation`.
- Added notification badge with ping animation for better visibility.
- Used `pb-safe` for safe area padding on mobile devices.
